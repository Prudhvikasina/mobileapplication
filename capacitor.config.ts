import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'People.Task.list',
  appName: 'ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
